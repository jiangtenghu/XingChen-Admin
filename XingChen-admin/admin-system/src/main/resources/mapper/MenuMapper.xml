<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.MenuMapper">

    <resultMap id="MenuResult" type="com.admin.system.domain.entity.Menu">
        <id property="id" column="id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="query" column="query"/>
        <result property="isFrame" column="is_frame"/>
        <result property="isCache" column="is_cache"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <!-- 根据用户ID查询菜单列表 -->
    <select id="selectMenusByUserId" parameterType="Long" resultMap="MenuResult">
        SELECT DISTINCT m.*
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        AND m.del_flag = 0
        AND m.status = '0'
        AND r.status = '0'
        ORDER BY m.parent_id, m.order_num
    </select>

    <!-- 根据角色ID查询菜单列表 -->
    <select id="selectMenusByRoleId" parameterType="Long" resultMap="MenuResult">
        SELECT m.*
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId}
        AND m.del_flag = 0
        AND m.status = '0'
        ORDER BY m.parent_id, m.order_num
    </select>

    <!-- 根据用户ID查询用户拥有的菜单树 -->
    <select id="selectMenuTreeByUserId" parameterType="Long" resultMap="MenuResult">
        SELECT DISTINCT m.*
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        AND m.del_flag = 0
        AND m.status = '0'
        AND r.status = '0'
        AND m.menu_type IN ('M', 'C')
        ORDER BY m.parent_id, m.order_num
    </select>

</mapper>